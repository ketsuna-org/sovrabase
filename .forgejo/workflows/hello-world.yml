name: Hello World

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  hello:
    runs-on: docker
    steps:
      - name: Echo Hello World
        run: echo "Hello World"
